# Hugo build artifacts
public/
resources/
.hugo_build.lock

# Paper Reading — locally cached PDFs (never pushed; fetched from arXiv on demand).
# annotations.json IS committed so highlights/notes show on the public site.
paper-reading/pdfs/

# Interest ratings (1-5 "有意思指数") are private — local-only, never pushed,
# so the public site never appears to rate/judge other people's papers.
paper-reading/data/interest.json
